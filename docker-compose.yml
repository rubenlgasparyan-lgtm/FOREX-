version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: arcarius
      POSTGRES_PASSWORD: arcarius123
      POSTGRES_DB: arcarius_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
